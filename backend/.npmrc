# TypeORM 0.3.x declares an optional peer range for better-sqlite3 (^7||^8||^9)
# that predates the Node-26-compatible better-sqlite3 11.x we pin. The peer is
# optional and the runtime API is compatible, so accept the newer version.
legacy-peer-deps=true
