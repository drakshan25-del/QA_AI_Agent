# V3 stack environment (root). Copy to .env for docker-compose. Never commit real secrets (SEC-002).
ENGINE_TOKEN=dev-engine-token
JWT_ACCESS_SECRET=sdf3jslaf-asdf43jkl-GJOJJ43J
JWT_REFRESH_SECRET=lksK%^-JKHO56-eifjolsjfo
QA_OLLAMA_BASE_URL=http://host.docker.internal:11434
QA_LLM_MODEL=qwen2.5:latest
SEED_ADMIN_EMAIL=rakshandangol93@gmail.com
SEED_ADMIN_PASSWORD=admin@12345
# GitHub PAT for pushing generated automation to the project repository and
# dispatching Actions workflows. Fine-grained token: select the target repo(s),
# grant "Contents: Read and write" + "Actions: Read and write". Classic token:
# 'repo' + 'workflow' scopes. Never commit a real token (SEC-002).
GITHUB_TOKEN=

# --- V3 additions -----------------------------------------------------------
# Engine call timeout (ms); local LLM generation can take minutes.
ENGINE_TIMEOUT_MS=600000
# Hard job timeout (ms) -> status `timed_out` (SRS §23.7).
JOB_TIMEOUT_MS=1800000

# Execution queue + admin-defined runtime limits (FR-V3-EXE-011/012).
EXEC_MAX_CONCURRENT=1
EXEC_MAX_TIMEOUT_SECONDS=1800
EXEC_MAX_RETRIES=3
EXEC_MAX_WORKERS=4
EXEC_MAX_SLOWMO_MS=2000

# Retention windows in days; 0 keeps records forever (FR-V3-ENT-011).
RETENTION_JOB_LOG_DAYS=0
RETENTION_EXECUTION_EVENT_DAYS=0
RETENTION_EVIDENCE_DAYS=0
RETENTION_SWEEP_INTERVAL_MINUTES=720
