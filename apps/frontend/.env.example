# Frontend environment (V2_CONTRACT §6). Copy to .env.local for local dev.
# Browser talks ONLY to the backend (FR-FE-004); no secrets belong here (SEC-002).
VITE_API_BASE=http://localhost:4000/api/v2
VITE_WS_BASE=ws://localhost:4000
